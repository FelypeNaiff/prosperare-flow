{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user account within the system.",
      "properties": {
        "id": { "type": "string" },
        "fullName": { "type": "string" },
        "email": { "type": "string" },
        "profile": { "type": "string", "enum": ["ADMINISTRADOR", "SÓCIO", "CONTADOR/GESTOR", "ASSISTENTE"] },
        "role": { "type": "string" },
        "status": { "type": "string", "enum": ["ATIVO", "INATIVO"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "departmentId": { "type": "string" },
        "departmentIds": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["id", "fullName", "email", "profile", "status", "createdAt"]
    },
    "Client": {
      "title": "Client",
      "type": "object",
      "description": "Client account details.",
      "properties": {
        "id": { "type": "string" },
        "corporateName": { "type": "string" },
        "cnpj": { "type": "string" },
        "email": { "type": "string" },
        "taxRegime": { "type": "string" },
        "healthScore": { "type": "number" },
        "obligationGroups": { "type": "array", "items": { "type": "string" } }
      }
    },
    "Process": {
      "title": "Process",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nomeProcesso": { "type": "string" },
        "situacao": { "type": "string" },
        "clienteId": { "type": "string" },
        "departamento": { "type": "string" },
        "prazo": { "type": "string", "format": "date-time" }
      }
    },
    "Task": {
      "title": "Task",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "status": { "type": "string" },
        "clientId": { "type": "string" }
      }
    },
    "ProcessModel": {
      "title": "ProcessModel",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nome": { "type": "string" }
      }
    },
    "Certification": {
      "title": "Certification",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "tipo": { "type": "string" },
        "clientId": { "type": "string" }
      }
    },
    "Notification": {
      "title": "Notification",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "message": { "type": "string" },
        "read": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" }
      }
    },
    "FinancialEntry": {
      "title": "FinancialEntry",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "valor": { "type": "number" },
        "tipo": { "type": "string" }
      }
    },
    "Contract": {
      "title": "Contract",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "value": { "type": "number" },
        "clientId": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/User" },
      "description": "User profiles indexed by Auth UID."
    },
    "/clients/{id}": {
      "schema": { "$ref": "#/backend/entities/Client" },
      "description": "Client database."
    },
    "/processes/{id}": {
      "schema": { "$ref": "#/backend/entities/Process" },
      "description": "Active processes/obligations."
    },
    "/tasks/{id}": {
      "schema": { "$ref": "#/backend/entities/Task" },
      "description": "Process checklist tasks."
    },
    "/processoModelos/{id}": {
      "schema": { "$ref": "#/backend/entities/ProcessModel" },
      "description": "Intelligent checklist models."
    },
    "/certifications/{id}": {
      "schema": { "$ref": "#/backend/entities/Certification" },
      "description": "Client certifications (CNDs)."
    },
    "/notifications/{id}": {
      "schema": { "$ref": "#/backend/entities/Notification" },
      "description": "User notification system."
    },
    "/chatMessages/{id}": {
      "schema": { "$ref": "#/backend/entities/ChatMessage" },
      "description": "Internal team communication."
    },
    "/financialEntries/{id}": {
      "schema": { "$ref": "#/backend/entities/FinancialEntry" },
      "description": "Cash flow transactions."
    },
    "/contracts/{id}": {
      "schema": { "$ref": "#/backend/entities/Contract" },
      "description": "Service contracts."
    }
  }
}